import React from 'react';
import { css } from '@emotion/core';

export default () => (
  <div
    css={css`
      display: flex;
      justify-content: center;
    `}>
    <h1
      css={css`
        margin: 0;
      `}>
      Gatsby Blog
    </h1>
  </div>
);
